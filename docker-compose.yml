services:
  web:
    build: .
    ports:
      - "8080:80"
    restart: unless-stopped

  # Future: add api and db services here
  # api:
  #   build: ./api
  #   ports:
  #     - "5000:5000"
  #   depends_on:
  #     - db
  #
  # db:
  #   image: postgres:16
  #   environment:
  #     POSTGRES_USER: bandtimeline
  #     POSTGRES_PASSWORD: changeme
  #     POSTGRES_DB: bandtimeline
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data
  #
  # volumes:
  #   pgdata:
